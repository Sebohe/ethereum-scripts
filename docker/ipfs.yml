version: '3.7'


volumes:
  ipfs-data:
  ipfs-staging:

services:
  core:
    image: "ipfs/go-ipfs:v0.4.18"
    deploy: 
      mode: global
    ports:
      - "4001:4001"
      - "0.0.0.0:5001:5001"
      - "8080:8080"
    volumes:
      - ipfs-data:/data/ipfs/
      - ipfs-staging:/export
